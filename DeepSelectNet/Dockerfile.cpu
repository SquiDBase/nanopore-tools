# DeepSelectNet requires Python 3.5 >= version <= 3.8
FROM --platform=linux/amd64 python:3.8 AS build

WORKDIR /app

RUN apt-get update && \
	apt-get install -y --no-install-recommends \
	build-essential \
	gcc \
	git

ARG UPSTREAM_REF=66429a8
RUN mkdir DeepSelectNet && cd DeepSelectNet && \
	git init && \
	git remote add origin https://github.com/AnjanaSenanayake/DeepSelectNet && \
	git fetch --depth 1 origin ${UPSTREAM_REF} && \
	git checkout FETCH_HEAD && \
	rm -rf .git

RUN pip3 install -r ./DeepSelectNet/requirements.txt \
	# Workaround "generated code is out of date and must be regenerated with protoc >= 3.19.0" error in trainer.py
	protobuf==3.20.* \
	# Workaround "module 'numpy' has no attribute 'object'" error in trainer.py
	numpy==1.19.5 \
	# Workaround "Matplotlib requires numpy>=1.20; you have 1.19.5" error in trainer.py
	matplotlib==3.6 && \
	# Workaround Pyslow "ValueError: numpy.ndarray size changed, may indicate binary incompatibility." error in inference.py
	# (Rebuild pyslow5 using older numpy)
	pip uninstall pyslow5 -y && \
    pip install pyslow5 --no-binary pyslow5

RUN useradd -m user

ENV PATH=$PATH:/app/DeepSelectNet/scripts:/app/DeepSelectNet/support

# Copy modified tester script
COPY inference_moreoutput.py /app/DeepSelectNet/scripts/inference_moreoutput.py

RUN for script in inference.py preprocessor.py trainer.py inference_moreoutput.py; do \
        chmod +x /app/DeepSelectNet/scripts/$script && \
        sed -i '1i#!/usr/bin/env python3' /app/DeepSelectNet/scripts/$script; \
    done

USER user

WORKDIR /workspace
